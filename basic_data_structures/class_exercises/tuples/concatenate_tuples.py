"""
Write a function concatenate_tuples that takes two tuples as input and returns a new tuple containing
elements from both input tuples concatenated together. For example:

    >>> concatenate_tuples((1, 2), (3, 4, 5))
    Output: (1, 2, 3, 4, 5)

BONUS: implement the same with loops
"""
from pathlib import Path

from basic_data_structures.class_exercises.utils import test_my_solution


def concatenate_tuples(tuple1: tuple, tuple2: tuple) -> tuple:
    return ()

if __name__ == '__main__':
    test_my_solution(Path(__file__))